services:
  # Backend Service
  - type: web
    name: 15market-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3010
      - key: NODE_ENV
        value: production
      # These should be set in the Render Dashboard for security, but listed here for clarity
      - fromContext: env
        key: PRIVATE_KEY
      - fromContext: env
        key: SESSION_MASTER_SECRET
      - fromContext: env
        key: ARC_CONTRACT_ADDRESS
      - fromContext: env
        key: REDIS_URL

  # Frontend Service (Static Site)
  - type: web
    name: 15market-frontend
    env: static
    rootDir: 15market
    buildCommand: npm install && npm run build
    staticPublishDir: dist
    envVars:
      - key: VITE_KEEPER_URL
        value: https://15market-backend.onrender.com
      - key: VITE_KEEPER_URL_ROUNDS
        value: https://15market-rounds.onrender.com

  # Rounds Backend Service
  - type: web
    name: 15market-rounds
    env: node
    rootDir: rounds-backend
    port: 3011
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: PORT
        value: 3011
      - key: NODE_ENV
        value: production
      - fromContext: env
        key: REDIS_URL
      - fromContext: env
        key: ADMIN_TOKEN
      - fromContext: env
        key: SESSION_MASTER_SECRET
      - fromContext: env
        key: PRIVATE_KEY
      - fromContext: env
        key: ROUNDS_CONTRACT_ADDRESS
